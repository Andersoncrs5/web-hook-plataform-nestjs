export enum OrganizationSort {
    ID = "id",
    
    NAME = "name",
    SLUG = "slug",
    STATUS = "status",
    USER_ID = "userId",

    VERSION = "version",
    CREATED_AT = "createdAt",
    UPDATED_AT = "updatedAt",
}